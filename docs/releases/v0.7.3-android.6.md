Requires LemonLoader Patcher 1.1.0 or later. Android remains the default runtime;
select the Bionic archive explicitly when that profile is needed.

Both .NET 11 runtime profiles remain development previews. This release adds
host regressions for startup rollback, JNI ownership, deployment I/O and hook
failure handling; these checks do not expand real-game or physical 16 KiB-page
device qualification.
